Example Tomcat setenv.sh fragment for Bog Standard.
Twin of setenv.example.sh.

Required: PGHOST, PGPORT, PGDATABASE, PGUSER, PGPASSWORD, BASE_URL, SESSION_SECRET,
SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS, SMTP_FROM.
Optional: TILE_URL, TILE_ATTR, UPLOAD_DIR, DISPOSABLE_DOMAINS.
